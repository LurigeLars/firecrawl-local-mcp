# Copy to public/gateway.env (gitignored). Random, at least 32 characters (URL-safe).
GATEWAY_SECRET=<random-43-chars>
RATE_PER_MIN=120

# Public hostname used by fc.ps1 url/selftest. Keep the real deployment value only in public/gateway.env.
PUBLIC_HOSTNAME=firecrawl.example.com
