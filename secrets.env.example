# Copy to secrets.env (gitignored). Random, at least 32 characters.
POSTGRES_PASSWORD=<random-32+-chars>
SEARXNG_SECRET=<random-64-hex>
# Optional: Google AI Studio key. Set = Gemini answers query/json/summary first (page text goes to Google).
GEMINI_API_KEY=
